"use client";

import React from "react";
import Link from "next/link";
import {
  Calendar,
  ArrowLeft,
  Newspaper,
} from "lucide-react";

const newsItems = [
  {
    id: 1,
    title: "IRS announces 2025 tax filing season dates",
    date: "2025-01-10",
    tag: "IRS Update",
    summary:
      "Key deadlines and new procedures impacting individual taxpayers this season.",
  },
  {
    id: 2,
    title: "Standard deduction and credits adjusted for inflation",
    date: "2024-11-05",
    tag: "Tax Planning",
    summary:
      "Important changes that may affect your refund or balance due next year.",
  },
  {
    id: 3,
    title: "E-File security tips for taxpayers",
    date: "2024-10-20",
    tag: "Security",
    summary:
      "How to protect your identity and avoid scams when filing your taxes online.",
  },
];

export default function NewsPage() {
  return (
    <main className="min-h-screen bg-slate-950 text-slate-50">
      <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-10 space-y-10">
        
        {/* Back link */}
        <div className="flex items-center justify-between gap-4">
          <Link
            href="/"
            className="inline-flex items-center gap-2 text-sm text-slate-300 hover:text-white transition"
          >
            <ArrowLeft className="h-4 w-4" />
            Back to homepage
          </Link>

          <Link
            href="/"
            className="text-xs px-3 py-1 rounded-full border border-slate-700 bg-slate-900/70"
          >
            eztax.app
          </Link>
        </div>

        {/* Header */}
        <header className="space-y-4">
          <div className="inline-flex items-center gap-2 rounded-full border border-emerald-400/30 bg-emerald-500/10 px-3 py-1 text-xs font-medium text-emerald-200">
            <Newspaper className="h-3 w-3" />
            Tax News & IRS Updates
          </div>
          <h1 className="text-3xl sm:text-4xl font-semibold tracking-tight">
            Latest news for U.S. taxpayers
          </h1>
          <p className="text-sm sm:text-base text-slate-300 max-w-2xl">
            Curated IRS announcements, tax law changes, and practical tips to
            stay compliant and avoid surprises at tax time.
          </p>
        </header>

        {/* News List */}
        <section className="space-y-4">
          {newsItems.map((item) => (
            <article
              key={item.id}
              className="rounded-2xl border border-slate-800 bg-slate-900/60 shadow-sm hover:border-emerald-500/50 transition group p-5 sm:p-6"
            >
              <div className="flex flex-wrap items-center justify-between gap-3 mb-3">
                <div className="flex items-center gap-3 text-xs text-slate-400">
                  <span className="inline-flex items-center gap-1">
                    <Calendar className="h-3 w-3" />
                    {new Date(item.date).toLocaleDateString("en-US", {
                      year: "numeric",
                      month: "short",
                      day: "numeric",
                    })}
                  </span>
                  <span className="h-1 w-1 rounded-full bg-slate-600" />
                  <span className="px-2 py-0.5 rounded-full border border-slate-700 bg-slate-900/80 text-[11px] uppercase tracking-wide">
                    {item.tag}
                  </span>
                </div>
              </div>

              <h2 className="text-lg sm:text-xl font-semibold mb-2 group-hover:text-emerald-300">
                {item.title}
              </h2>

              <p className="text-sm text-slate-300 max-w-2xl">
                {item.summary}
              </p>
            </article>
          ))}
        </section>

        {/* CTA */}
        <section className="mt-6 rounded-2xl border border-emerald-500/30 bg-emerald-500/10 p-5 sm:p-6">
          <h3 className="text-base sm:text-lg font-semibold mb-1">
            Need help understanding how this affects you?
          </h3>
          <p className="text-sm text-emerald-50/90 mb-3">
            Book a consultation and we'll review your IRS situation, notices,
            and planning opportunities.
          </p>
          <Link
            href="/#contact"
            className="inline-flex items-center justify-center rounded-full px-4 py-2 text-sm font-medium bg-emerald-400 text-slate-950 hover:bg-emerald-300 transition"
          >
            Talk to a tax pro
          </Link>
        </section>

      </div>
    </main>
  );
}
